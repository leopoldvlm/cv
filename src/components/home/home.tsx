import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <section class="text-black h-screen snap-start py-24">
        <div class="bg-gradient-to-r from-blue-200 via-cyan-100 to-cyan-200 w-[90vw] h-[80vh] mx-auto rounded-xl">
          
        </div>
    </section>    
    )
});