import { component$ } from '@builder.io/qwik';

export default component$(() => {

  return (
    <header /*class="h-16 w-full bg-white fixed flex flex-row"*/>
      <nav></nav>
      <div>

      </div>
    </header>
  );
});
